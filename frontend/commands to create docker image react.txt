# i folden med Dockerfile (for dunamis_react)
/frontend mappen av github repositoriet

# build custom image node.js react
#minwg64 / Linux
docker build -t dunamis_react:latest .

#windows
docker build -t dunamis_react:latest .

# finn imaget
docker images
